# So sánh tiến hóa codebase: `20260320` → `20260327`

File **độc lập** — tóm tắt khác biệt và cách kiểm chứng. Chi tiết từng nhánh xem:

- `20260320.md` — snapshot & review branch `20260320`
- `20260327.md` — snapshot & review branch `20260327`

---

## Kiểm chứng nhanh (git)

```bash
git fetch --all
git diff 20260320..20260327 --stat
git log 20260320..20260327 --oneline
```

---

## Tóm tắt thay đổi (theo diff)

| Hạng mục | `20260320` | `20260327` |
|----------|------------|------------|
| API upload | Không mount | `POST/GET /api/v1/upload/*` (ảnh, Excel, file tĩnh) |
| Dependencies | — | `multer`, `exceljs` |
| Schema `product` | Không có `sku` trong nhánh cũ | Field **`sku`** (required, unique) |
| Error handler app | `res.render('error')` | `res.send(err.message)` |
| File mới | — | `routes/upload.js`, `utils/upload.js`, có thể có file trong `uploads/` |

---

## Nâng cấp (evolution)

- **Nghiệp vụ:** import hàng loạt sản phẩm từ Excel + upload ảnh; tạo product + inventory trong transaction.
- **Dữ liệu:** SKU là khóa nghiệp vụ rõ ràng hơn.
- **Vận hành API:** lỗi toàn cục trả text thay vì render view — phù hợp API JSON.

## Không đổi trong diff (nợ kỹ thuật có thể còn)

Các file như `routes/users.js`, `routes/products.js`, `utils/authHandler.js` **không** nằm trong diff hai nhánh — bug/điểm yếu ghi trong `20260320.md` **có thể vẫn còn** trên `20260327` cho đến khi sửa riêng.

## Breaking / lưu ý tích hợp

- Client/seed tạo product phải gửi **`sku`** (hoặc đổi server để sinh SKU) nếu schema bắt buộc.
- Thêm route `/api/v1/upload` — contract API mở rộng.

---

## Khi cập nhật file này

Sau mỗi lần merge/rebase đáng kể: chạy lại `git diff 20260320..20260327` (hoặc `..HEAD`), sửa bảng và ngày ghi bên dưới.

**Ngày ghi bản này:** 2026-03-30
